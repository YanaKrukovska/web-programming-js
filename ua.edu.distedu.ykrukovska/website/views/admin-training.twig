<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title> {{ config.websiteTitle }}</title>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" rel="stylesheet"/>
    <style>
        .center {
            margin-left: 25%;
            margin-right: 25%;
            margin-top: 100px;
            width: 50%;
        }
    </style>
</head>
<body>
{% include 'navbar.twig' %}

<div class="center">
    <form id="add-training-form" action="/training/add?lang={{ lang.lang }}" method="post">
        <div class="form-group">
            <label for="name">{{ lang.trainingNameField }}</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="{{ lang.trainingNameFieldTip }}"
                   required>
        </div>
        <div class="form-group">
            <label for="description">{{ lang.trainingDescriptionField }}</label>
            <input type="text" class="form-control" id="description" name="description" placeholder="{{ lang.trainingDescriptionFieldTip }}"
                   required>
        </div>
        <div class="buttons">
            <button type="submit" class="btn btn-primary btn-xl">{{ lang.addTraining }}</button>
        </div>
    </form>
</div>
</body>
</html>
